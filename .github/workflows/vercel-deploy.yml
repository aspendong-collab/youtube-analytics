name: Trigger Vercel Deployment

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Vercel
        run: |
          echo "Push to main branch detected"
          echo "This workflow can be extended to trigger Vercel API"
          echo "Current commit: ${{ github.sha }}"
